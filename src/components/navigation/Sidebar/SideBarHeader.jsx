import { Box } from "@mui/joy";

function SideBarHeader() {
  return (
    <Box
      sx={{
        width: "100%",
        height: 100,
        display: "flex",
        justifyContent: "center",
        paddingTop: 2,
    }}
    >
      Sidebar
    </Box>
  );
}

export default SideBarHeader;